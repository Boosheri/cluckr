<!DOCTYPE html>
<html lang="en">

<head>
  <title>Cluckr</title>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="/css/style.css" rel="stylesheet">
</head>

<body>
  <header>

    <nav class="nav-bar">

    <a href="index"><img class="nav-image" src="/images/chicken.jpg" alt="cluckr chicken"></a>

      <button class="header-cluck"> <a href="/clucks/new">Cluck</a></button>

      <% if (username) { %>
      <span>
         <%= username %>
      </span>
      <form class="sign-out-form" action="/sign-out" method="POST">
        <input type="submit" value="Sign Out">
      </form>

      <% } else { %>
      <form class="sign-in-form" action="/sign-in" method="POST">
        <input type="submit" value="Sign In">
      </form>
      <% } %>

    </nav>

  </header>
  <main>